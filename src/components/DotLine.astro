---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span class={`inline-flex items-center gap-2 ${className}`} aria-hidden="true">
  <span class="h-px w-8 bg-copper"></span>
  <span class="h-1.5 w-1.5 rounded-full bg-copper"></span>
</span>
